ui = true

storage "file" {
  path = "/vault/data"
}

listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = 1
}

# Indirizzo raggiungibile dall'esterno del container (usato dal cluster k3s)
api_addr = "http://{{ ansible_host }}:8200"
